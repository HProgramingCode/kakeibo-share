# ソースフォルダ構成管理

修正したい内容から、最初に開くソースを探すための案内です。画面専用の部品は `src/app/**/_components`、複数画面で使う処理は `src/features` に置いています。

## ログイン・登録

認証画面を直すときは、[ログインページ](src/app/login/page.tsx) または [登録ページ](src/app/signup/page.tsx) から確認します。フォーム本体は [login](src/features/auth/login)・[signup](src/features/auth/signup)、認証処理は [actions](src/features/auth/actions) と [server](src/features/auth/server) です。

## グループ一覧・作成・招待参加

[グループ一覧](src/app/groups/page.tsx)、[作成](src/app/groups/new/page.tsx)、[招待参加](src/app/groups/join/page.tsx) が画面の入口です。招待URLと承諾処理は [invite](src/features/groups/invite)、一覧専用部品は [list](src/features/groups/list) を見ます。

## グループ詳細画面

[グループ詳細ページ](src/app/groups/[id]/page.tsx) が全体の組み立てです。画面固有のヘッダー・ダッシュボード・履歴は [_components](src/app/groups/[id]/_components)、表示用の整形は [_lib](src/app/groups/[id]/_lib) にあります。

## 支出の登録・編集

支出入力・編集のフォーム、日付・カテゴリ・立替者選択、参加者と割り勘の処理は [expenses/form](src/features/expenses/form) に集約しています。保存処理は [expense actions](src/features/expenses/actions)、DBアクセスは [expense repositories](src/features/expenses/repositories) です。

## 支出一覧・履歴

一覧行、一覧表示、編集シート、一覧用の型は [expenses/feed](src/features/expenses/feed) にあります。履歴画面そのものは [history components](src/app/groups/[id]/_components/history) です。

## 精算・送金額計算

二者間相殺と送金額の表示は [settlement/balance](src/features/settlement/balance) を確認します。精算確定の入口は [settlement actions](src/features/settlement/actions)、保存用RPC呼び出しは [settlement repositories](src/features/settlement/repositories) です。

## グラフ

[グラフページ](src/app/groups/[id]/charts/page.tsx) が入口です。グラフUI・月選択・集計ロジックは [settlement/charts](src/features/settlement/charts)、グループ画面側のヘッダーは [groups/charts](src/features/groups/charts) にあります。

## Supabase・共通UI・画面遷移

Supabaseのクエリは各 feature の `repositories`、クライアントとmiddlewareは [server/supabase](src/server/supabase) を確認します。SQLは [supabase/migrations](supabase/migrations) にあります。共通UI・表示整形・ルート定義は [shared](src/shared) にあります。
