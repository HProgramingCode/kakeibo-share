# dependencies
/node_modules

# next.js
/.next/
/out/

# env & secrets（本番・ローカルともリポジトリに含めない。PC のみで管理）
.env
.env.local
.env*.local
.env.production
*.pem
*.p12

# deploy / build ツールのローカル出力
.vercel
.turbo

# TypeScript（incremental キャッシュ）
*.tsbuildinfo

# logs / misc
npm-debug.log*
.DS_Store

# supabase CLI の一時ファイル・ローカル用シークレット（OAuth 等は config.toml の env() 参照）
supabase/.temp/
supabase/.env

# IDE・エージェント（デプロイ時は不要、PC のみで管理）
/.vscode/
/.cursor/
.claude/
CLAUDE.md
Claude.md
/.idea/
/.agents/
/skills-lock.json

# プロジェクト内ドキュメント・参照サンプル（アプリ実行には不要、PC のみで管理）
docs/
/sample/
